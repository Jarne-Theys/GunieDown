This folder should contain upgrades ONLY containing stat components, as these will be used by the AI (which currently can only use stat upgrades).
Upgrades with both stat and ability components should not be placed here.
